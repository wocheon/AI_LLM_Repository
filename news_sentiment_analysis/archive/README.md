# Archive

현재 애플리케이션 흐름에서는 사용하지 않는 초기 감성 분석 배치 구현을 보관합니다.

| 디렉터리 | 내용 |
| --- | --- |
| `kobert_batch` | KoBERT 기반 감성 분석 배치 |
| `koelectra_batch` | KoELECTRA 기반 감성 분석 배치 |

새 기능을 이 디렉터리에 추가하지 않습니다. 현재 애플리케이션 코드는 `../application`, 데이터 구축 코드는 `../data_pipeline`에서 관리합니다.

