[database]
host = mariadb-main
user = appuser
password = appuserpass
db = crawler_sentiment_analysis
charset = utf8

[elasticsearch]
host = elasticsearch
port = 9200
scheme = http
summary_index = article_summary

[queries]
; %%s -> %s 로 변경
model_data_query = SELECT a.id, k.keyword, k.category, a.title, LEFT(a.content, 200), a.url, r.sentiment, r.score, a.published_at, a.collected_at FROM crawler_article_list a JOIN sentiment_results r ON a.id = r.article_id JOIN crawler_keyword_list k ON a.keyword_id = k.id WHERE r.model_name = %s ORDER BY a.id DESC LIMIT 1000

; %%s -> %s 로 변경 (두 군데 모두)
model_comparison_query = SELECT a.id, k.keyword, k.category, a.title, LEFT(a.content, 100), a.url, a.published_at, r1.sentiment, r1.score, r2.sentiment, r2.score FROM crawler_article_list a JOIN crawler_keyword_list k ON a.keyword_id = k.id JOIN sentiment_results r1 ON a.id = r1.article_id AND r1.model_name = %s JOIN sentiment_results r2 ON a.id = r2.article_id AND r2.model_name = %s ORDER BY a.id LIMIT 500

[models]
; URL 슬러그 = 실제 DB에 저장된 모델명
kobert = kobert
koelectra = koelectra
qwen3 = qwen3

[sentiments]
; 모델별 감정 목록 (콤마로 구분)
kobert = positive, negative, neutral
koelectra = positive, negative, neutral
qwen3 = positive, negative, neutral


[emojis]
; 감정별 이모지 매핑 (공통)
angry = 😠
fear = 😳
happy = 😊
positive = 😊
tender = 😐
negative = 😢
sad = 😢
anxious = 😰
embarrassed = 😳
heartache = 💔
neutral = 😐