# 베이스 이미지로 Python 3.9 이상 사용 권장
FROM python:3.9-slim

# 작업 디렉토리 설정
WORKDIR /app

# 의존성 복사 및 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 모델 파일 및 코드 복사
COPY . .

# FastAPI 앱 실행 (Uvicorn 사용)
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]

